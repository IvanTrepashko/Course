@model List<course.ViewModels.OrderViewModel>

@{
    ViewData["Title"] = "Details";
}

@{ 
    Layout = "_AdminLayout";
}

<h3>@ViewBag.MasterName</h3>
<div>
    <hr />
    <table class="table table-hover table-borderless">
        <thead>
            <tr>
                <th>
                    Категория
                </th>
                <th>
                    Материал
                </th>
                <th>
                    Стоимость
                </th>
                <th>
                    Имя клиента
                </th>
                <th>
                    Дата заказа
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Category)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Material)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Cost)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ClientName)
                    </td>
                    <td>
                        @{
                            var localtime = item.OrderingTime.UtcDateTime.ToLocalTime();
                            @Html.DisplayFor(modelItem => localtime);
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
<div>
    <a class="btn btn-info" asp-action="Index">Назад</a>
</div>
